# Simple Transfer File Server
# No third-party packages required — only Python 3.8+ stdlib is used.
# tkinter must be available (it ships with the standard macOS/Windows Python installer).
#
# On some Linux distros, install it separately:
#   Ubuntu/Debian: sudo apt install python3-tk
#   Fedora:        sudo dnf install python3-tkinter
